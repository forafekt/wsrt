import fs from 'node:fs/promises';import process from 'node:process';import {spawn}from'node:child_process';import {CapabilityRegistry,type ProcessHandle,type RuntimeInstance,type RuntimeProvider}from'@wsrt/capabilities'
export class NodeRuntimeProvider implements RuntimeProvider{readonly id='node';async detect(){return{available:true,version:process.version}}async create():Promise<RuntimeInstance>{const children=new Set<ProcessHandle>();const capabilities=new CapabilityRegistry().provide('filesystem',{readText:(file)=>fs.readFile(file,'utf8'),writeText:(file,value)=>fs.writeFile(file,value),exists:async(file)=>fs.access(file).then(()=>true,()=>false)}).provide('environment',{all:()=>({...process.env}),get:(name)=>process.env[name]}).provide('process',{cwd:()=>process.cwd(),pid:()=>process.pid,platform:()=>process.platform}).provide('http',{fetch:(input,init)=>fetch(input,init)}).provide('timers',{delay:(ms,signal)=>new Promise((resolve,reject)=>{const timer=setTimeout(resolve,ms);signal?.addEventListener('abort',()=>{clearTimeout(timer);reject(signal.reason)},{once:true})})}).provide('logger',{log:(level,message,attributes)=>console[level==='warning'?'warn':level==='debug'?'debug':level](message,attributes??'')}).provide('spawn',{spawn:(request)=>{const child=spawn(request.command,[...request.args],{cwd:request.cwd,env:{...process.env,...request.environment},shell:request.shell??false,stdio:'inherit',signal:request.signal});let running=true;const handle:ProcessHandle={pid:child.pid??-1,get running(){return running},exit:new Promise(resolve=>child.once('exit',(code,signal)=>{running=false;children.delete(handle);resolve({code,signal})})),terminate:(signal='SIGTERM')=>child.kill(signal as NodeJS.Signals)};children.add(handle);return handle}});return{provider:this.id,capabilities,dispose:async()=>{for(const child of children)child.terminate();await Promise.allSettled([...children].map(child=>child.exit))}}}}
