# @wsrt/workspace-intelligence

Transport-neutral, immutable semantic workspace contracts and composition services for WSRT. This package consumes existing workspace authorities; it does not scan repositories, create control planes, or define a wire protocol.
