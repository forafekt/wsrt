# @wsrt/workspace-session

Owns the local, versioned IPC boundary between one authoritative workspace host and its CLI, dashboard, MCP, and IDE clients. Discovery records contain connection metadata only; live state remains in the host-owned control plane.
