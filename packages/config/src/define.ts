import type { ProjectConfig, WsrtConfig } from '@wsrt/types'

export function defineWorkspace(config: WsrtConfig): WsrtConfig {
  return config
}

export function defineProject(config: ProjectConfig): ProjectConfig {
  return config
}
