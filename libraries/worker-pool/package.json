{
  "name": "@wsrt/worker-pool",
  "private": false,
  "version": "0.1.0",
  "type": "module",
  "description": "Typed Node.js worker_threads pool with dynamic scaling, retries, cancellation, events, and metrics.",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "examples",
    "README.md"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./worker": {
      "types": "./dist/worker.d.ts",
      "import": "./dist/worker.js"
    }
  },
  "scripts": {
    "build": "pnpm run build:types && pnpm exec tsc -p tsconfig.build.json",
    "build:types": "pnpm exec tsc -p tsconfig.build.json --emitDeclarationOnly",
    "test": "pnpm run build && node --test tests/*.test.mjs",
    "typecheck": "pnpm exec tsc -p tsconfig.json --noEmit && pnpm exec tsc -p tsconfig.test.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.12.0",
    "typescript": "~5.9.3"
  },
  "license": "ISC",
  "packageManager": "pnpm@10.12.4"
}
